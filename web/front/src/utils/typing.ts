export namespace Typing {
    export interface Rule {
        acc: number,
        coef: number,
        importance: number,
        index: number,
        name: string,
        predctionType: string,
        rule: string,
        support: number,
        type: string
    }
}